{
  "name": "cypress-poc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cypress:open:qa": "cypress open --env environment=qa",
    "cypress:open:trn": "cypress open --env environment=trn",
    "cypress:run:qa": "cypress run --env environment=qa",
    "cypress:run:trn": "cypress run --env environment=trn"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^23.2.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.0.0",
    "@faker-js/faker": "^10.0.0",
    "cypress-dotenv": "^3.0.1",
    "dotenv": "^17.2.2"
  }
}